template_sets:
  global-system-templates:
    label: global-system-templates
    base_path: templates
    order: 1000
    templates:
      index:
      individual:
      page:
      archive:
      custom:
      widget:
      system:

